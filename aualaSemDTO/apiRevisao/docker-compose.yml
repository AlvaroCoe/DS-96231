version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: mysql_api_alunos
    restart: always
    environment:
      MYSQL_DATABASE: escola_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"